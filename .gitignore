# Build output.
/build/
/out/

# Generated from raylib.toml on every configure. Never committed, so it cannot
# drift: the .toml is the only source of truth.
/cmake/generated/
/include/app_config.h

# Editor and OS.
.cache/
compile_commands.json
.DS_Store
Thumbs.db
*.swp
